version: "3.7"

services:
  fluent-bit:
    image: fluent/fluent-bit:1.8
    volumes:
      - ./fluent-bit.conf:/fluent-bit/etc/fluent-bit.conf
      - ./lib/libldserverapi.so:/usr/lib/libldserverapi.so
      - ./include/launchdarkly/:/usr/include/launchgdarkly
      - ./ld.lua:/ld.lua
    